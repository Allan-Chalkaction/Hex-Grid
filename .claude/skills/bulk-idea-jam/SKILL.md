---
name: bulk-idea-jam
description: "TOMBSTONE — compose + jam the whole inbox moved to /sweep (ADR-081 → ADR-112 Wave 3). Run /sweep."
user_invocable: true
---

# /bulk-idea-jam — RETIRED (compose + jam now in /sweep)

Replaced by `/sweep` (ADR-081 absorbed it into `/bulk-jam`; ADR-112 Wave 3 moved cluster+jam convergence to
`/sweep`). The whole-inbox compose + jam-per-cluster convergence is now `/sweep`'s in-skill convergence — run
`/sweep` and answer the `new-cluster` / `ingest-to-jam` verdicts.

(ADR-081; tombstone — delete after one release.)
