---
name: idea-compose
description: "TOMBSTONE — composing/grounding moved to /sweep (ADR-081 → ADR-112 Wave 3). Ground + converge an idea: run /sweep."
user_invocable: true
---

# /idea-compose — RETIRED (grounding now in /sweep)

Replaced by `/sweep` (ADR-081 absorbed it into `/idea-jam`; ADR-112 Wave 3 moved convergence to `/sweep`).
Grounding a thin spark into the converged thesis is now part of `/sweep`'s in-skill convergence (compose step)
— run `/sweep` and answer the `ingest-to-jam` / `new-cluster` verdicts.

(ADR-081; tombstone — delete after one release.)
