---
description: "TOMBSTONE — folded into /doctor (ADR-081). Audit CLAUDE.md for drift + staleness: /doctor --claude-md."
---

# /maintain-project-instructions — RETIRED (folded into /doctor)

Replaced by `/doctor` (ADR-081). The CLAUDE.md audit (global rules-drift + project-specific staleness of
paths, scripts, deps, imports) is now `/doctor --claude-md` (and runs as Step 2 of the default `/doctor`).

(ADR-081; tombstone — delete after one release.)
