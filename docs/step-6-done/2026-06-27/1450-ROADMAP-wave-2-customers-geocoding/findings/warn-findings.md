# WARN findings (ADR-086 — informational, no halt)

- **WAVE-BUDGET-customers-geocoding**: WAVE CONTEXT BUDGET (ADR-086) — wave 'customers-geocoding': predicted implementer consumption ~266,016 tokens vs budget 140,000 (calibrated basis: FIXED_OVERHEAD 60,000 + ~80,000 effective task context — ADR-086 D1/D3, telemetry-calibrated) = 190% of budget. This candidate wave (8 ticket(s), 14 planned file(s)) is predicted to EXCEED the budget AT PLANNING TIME. ADR-086: PROPOSE the split at the dependency seam (the slicer cut), or the operator may knowingly accept the over-budget wave (a recoverable, logged bet — per-ticket commits + thin manifest). File byte sizes were not all supplied via args, so a 8192-byte/file fallback was used for 14 file(s) — the prediction is coarse; pass per-file bytes for precision.
