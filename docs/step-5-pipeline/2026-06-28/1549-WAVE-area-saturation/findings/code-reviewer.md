# Code Review — Wave 4 (area-saturation) · Verdict: APPROVE
AS-T1..T6 implement the client-side H3 saturation heatmap cleanly + ADR-004-compliant. effectiveRadiusMi refactor render-equivalent for W3 (parity test); compute correct (overlap-weighted, same-vertical, off/null excluded, customer_id/self_conflict ignored, centroid-in-circle boundary); no supabase in coverage.ts; first paint W3-identical; no any; deck.gl umbrella import. All findings DISMISS (nits): CR-001 zone pre-filter by centroid drops a far large-radius zone (ADR-004 D2-sanctioned, AC-013 tests it as desired); CR-002 redundant coverage>=1 re-filter in saturationLayer; CR-003 "near center" wording (AC-025 mandated string). `_criterion_match_: none`.
