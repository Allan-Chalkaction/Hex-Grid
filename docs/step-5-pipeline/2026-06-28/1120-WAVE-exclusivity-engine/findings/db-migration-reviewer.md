# Migration Review — 0003_exclusivity_engine.sql · Verdict: APPROVE
Additive, zero-downtime: nullable customer.vertical + composite index + idempotent jsonb backfill; create-or-replace site_geo (security_invoker preserved, +3 cols); two security_invoker conflict RPCs (pinned search_path); grants mirror place_site. Predicate matches ADR Decision 2 (bidirectional max(A,B) ST_DWithin geography meters, >0 guard, same non-null vertical, self-excluded). exclusivity_radius_mi NOT re-added; site.vertical untouched. Schema-drift TS↔SQL consistent.
- **MR-001 (LOW, APPLY) — documented reverse path `alter table customer drop column vertical` is silently data-destructive** (picker-set verticals not mirrored in attributes are lost). Add a one-line DATA-LOSS caveat to the reverse comment (no code change).
CREATE INDEX non-concurrent is correct (can't run CONCURRENTLY in a txn; customer table small). Reverse correctly documented as drop view+create (create-or-replace can't drop view cols).
