# Performance Review — Wave 3 · Verdict: HAS_ISSUES (both ADR-anticipated, forward-carryable, _criterion_match_:none)
Rendering layer correct (recolor on data-change via updateTriggers, never per-frame). Two compounding server costs:
- **PR-001 (WARN, DEFER) — dynamic GREATEST() threshold defeats the GIST index → seq scan.** `0003 conflicts_at:124-132`: ST_DWithin threshold depends on the scanned row's exclusivity_radius_mi/is_zone_on, so ST_Expand bound is row-correlated → planner can't use site_geog_gist. Fix: add a CONSTANT pre-filter `ST_DWithin(s.geog,p_geog, 3*1609.344)` (picker max = 3 mi) THEN the exact GREATEST recheck. (3 must track RADIUS_OPTIONS max — document.)
- **PR-002 (WARN, DEFER) — App whole-tenant pass = N RPC round-trips, each a tenant scan (O(N²)) per data change** (App.tsx:28-42). On data-change not per-frame (confirmed). Fix: one set-returning `tenant_conflicts()` RPC (single spatial self-join + the PR-001 constant bound) → 1 round-trip. PR-001+PR-002 compound; apply together.
- **Spec scale claim "~10k sites/tenant" is NOT supportable as shipped — correct to "hundreds" (ADR's bound).** Fine to ~hundreds/tenant now.
- Suggestions (DEFER): PR-003 site_geo fetched twice/reload (App+CustomerList); PR-004 sequential add-check loop; PR-005 customer(tenant_id,vertical) index unused by current plan (keep, cheap); PR-006 deck.gl barrel import.
