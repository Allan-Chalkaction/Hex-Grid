# Implementer — T-002

STATUS: complete
SHA: 3c0afa9faf24e5dd0cbb9dd538a91e45bb7e4467
FILES: supabase/migrations/0001_init_postgis_schema.sql

Single raw-SQL migration built verbatim per ADR-001 in binding order: extension -> tables -> GIST index -> auth_tenant_ids() helper -> per-table RLS policies. VALIDATED LIVE against a started Supabase stack (Docker): `supabase start`/`db reset` applied with no SQL error; PostGIS 3.3; all four tables present; site.geog=geography, site.attributes=jsonb; site_geog_gist index present; geocode_cache has ZERO tenant_id columns; RLS enabled on all four. Ran a real two-tenant isolation test (two auth.users + tenants + memberships + sites, transaction-local `set role authenticated` + request.jwt.claims): user A sees exactly 1 site (Site A1) + 0 of B's Site B1; user B sees only Site B1 + the shared geocode_cache row A inserted; `select membership` raises NO infinite-recursion; anon sees 0 across site/tenant/membership. AC-009 hardening: discovered Supabase's pg_default_acl auto-grants EXECUTE on new public functions to anon/authenticated/service_role, which `revoke ... from public` does NOT strip — so I added `revoke execute ... from anon` (additive, ADR-aligned 'grant tightly' intent; harmless since the body keys off auth.uid()=NULL for anon). Verified runtime grantees are now authenticated/postgres/service_role only (anon removed). search_path pinned `public, pg_temp`; prosecdef=t. No anon policy on the three tenant tables (verified via pg_policies). AC-009 grep `to anon|role.*anon` returns clean (reworded the explanatory comment so it doesn't false-trip).
