# Code Review — Wave 6 (embed-harden) · Verdict: APPROVE
EH-T1..T4 match ADR-006/spec exactly; tsc clean, tests green; supabase-js-free public surface verified (grep clean). Load-bearing checks all PASS: self-registered Supabase default = byte-identical no-configure behavior; auth.ts/tenant.ts genuinely delegate; AuthGate type-only delta; listMemberships excluded from TenantProvider; over-engineering line held (no registry/DI/selection/flag/maxBounds); index.ts pure re-export (supabase @internal); vite-env.d.ts declares both ZCTA vars; no W2-5 regression.
- CR-001 (nit, DISMISS) delegators changed async fn → plain promise-returning fn; the unreachable no-provider branch would throw sync rather than reject. Statically unreachable (importing a delegator triggers self-registration); public Promise contract unchanged. Leaner form is fine.
