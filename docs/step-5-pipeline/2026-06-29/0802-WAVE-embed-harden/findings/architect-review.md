# Architect Review (FINAL) — Wave 6 (embed-harden) vs ADR-006 · Verdict: SOUND (ship as-is)
All 6 ADR-006 decisions honored + the cto SIMPLIFY override. (1) providers.ts AuthProvider/TenantProvider match real usage (listMemberships excluded), configureIdentity single injection, auth.ts/tenant.ts delegate, Supabase self-registers (active??default) — geocoder.ts precedent generalized; approved improvement over the D1 sketch (split default/active holders → "swap auth, leave tenant default" trivially correct + explicit throw neutralizes ordering risk). (2) supabase-js-free boundary REALIZED (providers.ts imports zero supabase-js; AppSession/Unsubscribe; the test fake imports zero supabase-js). (3) barrel + embed-contract.md ("reference, not negotiated"). (4) AK/HI aria-label only (no maxBounds). (5) ZCTA label helper + env decls. (6) no migration/DB. Over-engineering guard held; cross-wave clean. 3 informational notes, no action.
