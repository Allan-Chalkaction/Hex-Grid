# Wave 5 — reference-overlays — per-ticket build prompts

Build in dependency order. Single wave writer; each source file owned by one ticket (no shared-sink collisions). Full detail: run-folder spec.md (24 ACs), adr.md (ADR-005), ui-spec-addendum.md. No migration/backend. Project is node-only (no jsdom/RTL) — verify components by pure-logic/layer-config tests + inspection (do NOT stand up an RTL harness; that's a logged deferral). Reuse W2/W3/W4 token vocabulary + VERTICAL_OPTIONS/verticalLabel (import, never re-author).

Graph: RO-T1 → RO-T2 ; RO-T3 ; RO-T4 ; RO-T5 ← {T2,T3,T4} ; RO-T6 ← T1 ; RO-T7 ← {T5,T6}.

## RO-T1 — verticalStyle.ts palette + tests · deps: [] · AC-001/002/003 · code-reviewer
`src/lib/verticalStyle.ts`: `VERTICAL_COLORS` (module-const Record<token,[r,g,b]>) over the 8 VERTICAL_OPTIONS — gas[194,87,10] grocery[162,28,175] pharmacy[15,118,110] qsr[190,24,93] fitness[77,124,15] automotive[146,64,14] banking[71,85,105] hotel[126,34,206]; `VERTICAL_NEUTRAL=[107,114,128]` for null/unknown. Import VERTICAL_OPTIONS from customers.ts (never re-author). Avoid the reserved triples (conflict-red [176,0,32]; saturation [198,219,239]/[107,174,214]/[21,88,176]; prospect-green [19,115,51]). Tests: every token has a color; neutral correct; no value collides with a reserved triple (fixture); '', null, unlisted → neutral never undefined.

## RO-T2 — sitePinsLayer color-by-vertical + opt-in filter + tests · deps: [RO-T1] · AC-004/005/006 · code-reviewer
`src/components/sitePinsLayer.ts`: signature `sitePinsLayer(sites, { selectedVertical, filterToVertical })`. `getFillColor: d => VERTICAL_COLORS[d.vertical ?? ''] ?? VERTICAL_NEUTRAL` (opaque 3-tuple; keep 1px white stroke + located filter). Always-on color. Filter: `(filterToVertical && selectedVertical) ? located.filter(s=>s.vertical===selectedVertical) : located`. Keep id 'site-pins'; updateTriggers key (if any) CONSTANT. Tests (deck.gl constructs w/o GL, per saturationLayer.test): per-datum color incl. null→neutral; the three filter cases (on+gas→gas only; off→all; on+null→all); id stable.

## RO-T3 — capitals/metros JSON + referenceLabelsLayer + tests · deps: [] · AC-007/008/009/010/011 · code-reviewer
`src/data/capitals.json` (50 {name,state,lat,lng}, unique 2-letter states, CONUS+AK/HI bounds); `src/data/metros.json` (CBSA pop≥250k, 2020 vintage, [110,180] entries {name,lat,lng,pop} pop>=250000); `src/data/metros.source.md` provenance. `src/components/referenceLabelsLayer.ts` (deck.gl TextLayer factory from 'deck.gl' umbrella) `capitalsLayer(data)`/`metrosLayer(data)`: getText name, getPosition [lng,lat], sizeUnits pixels, white sdf halo (fontSettings.sdf:true, outlineWidth 2, outlineColor [255,255,255,255]), anchor middle/bottom, pickable false. Tiers: capitals size13 color[40,40,40] weight700; metros size11 color[85,85,85] weight400. Metros: CollisionFilterExtension + capitals higher getCollisionPriority; export `shouldShowMetros(zoom)=zoom>=5` (MapShell consumes). Tests: JSON counts/keys/types/bounds; layer instanceof TextLayer + accessors + sdf/outline/anchors/pickable + tier values; min-zoom predicate.

## RO-T4 — zctaSource.ts (env-gated, graceful degradation, click-to-zip) + tests + runbook · deps: [] · AC-012/013/014 · code-reviewer · ⚠️ OPERATOR-DEPENDENCY
`src/components/zctaSource.ts` MapLibre-native (NOT deck MVTLayer): `zctaConfigured()=!!import.meta.env.VITE_ZCTA_TILES_URL`; `addZctaSource(map)` on load (isStyleLoaded guard) → vector source from the env URL + `zcta-fill` (fill #6b7280 opacity ~0.04) then `zcta-line` (#6b7280 zoom-interp), both visibility:none; NO-OP (no source/request/console-error/layout-shift) when unconfigured; `setZctaVisible(map,on)` flips visibility; click→queryRenderedFeatures(['zcta-fill'])→read ZCTA5 from pinned key (ZCTA5CE20/GEOID20 + 5-digit-probe fallback)→maplibregl.Popup 'ZIP {zcta5}'. Token env-only (never hardcoded). `.env.example` + `docs/zcta-tiles-setup.md` runbook (Census TIGER zcta520 → tippecanoe → HTTP-Range bucket → set env). Tests (mock import.meta.env): configured flag; unconfigured no-op (no addSource/addLayer); configured adds source once + both layers visibility:none w/ paint; setZctaVisible flips both; id resolver pinned-key + fallback. FLAG: live tile render + live popup unverifiable locally without VITE_ZCTA_TILES_URL — build verifies wiring + unset path.

## RO-T5 — MapShell mount (z-order) + ZCTA source mount + new props · deps: [RO-T2,RO-T3,RO-T4] · AC-019/020/023 · code-reviewer, ui-review
Extend MapShell overlay.setProps conditional-spread seam (do NOT rewrite). New props: showCapitals/showMetros/showZcta/showZones/filterToVertical (+ selectedVertical, viewport zoom). Deck array z-order: `[ ...(showHeatmap&&sel?[saturationLayer]:[]), ...(showProspecting&&sel?[prospectLayer]:[]), ...(showZones?[siteZonesLayer]:[]), sitePinsLayer(sites,{selectedVertical,filterToVertical}), ...(showMetros&&zoom>=5?[metrosLayer(metros)]:[]), ...(showCapitals?[capitalsLayer(capitals)]:[]) ]` — labels LAST (capitals top); all reference layers conditional-spread → byte-identical to W4 when off. Add filterToVertical to the effect deps. Mount ZCTA via addZctaSource(map) on load + setZctaVisible(map,showZcta) on change (MapLibre-native, beneath the deck overlay → ZIP below pins). Record the 4-check legibility checklist (halos/pins-crisp/ZCTA-subtle/all-above-read) in the run folder for ui-review.

## RO-T6 — SaturationPanel → "Map layers" panel (in-place) + index.css + tests · deps: [RO-T1] · AC-015/016/017 · code-reviewer, ui-review, accessibility-auditor
Refactor SaturationPanel.tsx IN PLACE (keep .saturation-panel shell; NOT a new panel). `<h2>Map layers</h2>`; shared vertical `<select>` label 'Vertical' (''→null + VERTICAL_OPTIONS — the ONE control); 'Show only this vertical's sites' checkbox (filterToVertical, native disabled when no vertical); `<fieldset>` Reference (capitals/metros/ZIP — ZIP input native-disabled + aria-describedby 'Configure a ZCTA tile source (VITE_ZCTA_TILES_URL)…' when !zctaConfigured()); `<fieldset>` Analysis (Site zones showZones default-on, Saturation heatmap kept, Highlight open areas kept — last two disabled w/o vertical); collapsible `<details>` vertical color legend (VERTICAL_OPTIONS rows + 'No vertical' neutral; swatch aria-hidden, text is SR carrier, dynamic inline bg from VERTICAL_COLORS — sanctioned); KEEP the W4 .sat-legend numeric saturation legend + aria-live summary (seeded-empty no-vertical) + jump button. Preserve W4 a11y VERBATIM (useId, native disabled never aria-disabled). index.css: .layers-fieldset/.vertical-legend/.zcta-popup (semantic + literal hex). Tests: prefer pure-logic; if a render test is needed, follow the project's node posture — do NOT add jsdom/RTL (deferred). At minimum unit-test any extracted pure helpers + grep-assert VERTICAL_OPTIONS imported not redeclared.

## RO-T7 — App wiring + integration + wave code-review · deps: [RO-T5,RO-T6] · AC-018/021/022/024 · code-reviewer, ui-review
App.tsx: lift showCapitals/showMetros/showZcta (default off), filterToVertical (off), showZones (ON) alongside the W4 toggles; selectedVertical + viewport zoom already lifted. Thread setters/state to the panel; layer props to MapShell; showZcta via setZctaVisible (MapLibre-native, not deck array). AC-021: ONE shared selectedVertical drives saturation AND (when filterToVertical) the pin filter — exactly one vertical control/state (grep-assert). AC-022 wire-to-consumer: VERTICAL_COLORS in sitePinsLayer; capitalsLayer/metrosLayer in MapShell; addZctaSource/setZctaVisible in MapShell; panel controls bound to lifted App state (toggling a checkbox changes the rendered layer set). AC-024 wave code-review: no re-authored VERTICAL_OPTIONS; no hardcoded tile URL/token (env-only — grep); no MVTLayer; no new floating panel; no Tailwind/CSS-vars. Verify typecheck/lint/test green.
