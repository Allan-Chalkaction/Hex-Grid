# Wave 4 — area-saturation — per-ticket build prompts

Build in dependency order. One sequential wave writer; shared sinks (coverage.ts, index.css, App.tsx) serialized by depends_on. Full detail: run-folder spec.md (30 ACs), adr.md (ADR-004), ui-spec-addendum.md. No migration / no backend this wave.

---

## AS-T1 — effectiveRadiusMi helper + siteZonesLayer refactor + parity test · depends_on: []
**Files:** src/lib/coverage.ts, src/lib/coverage.test.ts, src/components/siteZonesLayer.ts · **ACs:** AC-001,002,003 · **Gates:** code-reviewer, performance-reviewer
Create `src/lib/coverage.ts` exporting `effectiveRadiusMi(site): number = is_zone_on ? (exclusivity_radius_mi ?? 0) : 0` (miles) — the exact W3 rule inlined today at siteZonesLayer.ts:34-41. Refactor siteZonesLayer's data filter to consume `effectiveRadiusMi(s) > 0` (kill the inline predicate) so W3 circles + W4 heatmap can't drift. W3 zone tests still pass. coverage.test.ts: off/null/zero/positive unit cases (AC-001); parity test pinning the helper to the W3 render (effectiveRadiusMi>0 iff a circle draws; ×1609.344 = the rendered meters radius, siteZonesLayer.ts:52) so a one-sided edit breaks the test (AC-003).

## AS-T2 — coverage compute core + h3-js + unit tests · depends_on: [AS-T1]
**Files:** src/lib/coverage.ts, src/lib/coverage.test.ts, package.json · **ACs:** AC-004..011,013,015,028,030 · **Gates:** code-reviewer, performance-reviewer
Add `h3-js` to package.json dependencies (AC-028). Implement: `resolutionForZoom(zoom)` clamped (zoom<5→4, 5-7→6, 8-10→7, >10→8; out-of-range clamps) (AC-010); viewport tessellation + overlap-weighted compute → `{h3, coverage}[]`: pad bounds ~20%, `polygonToCells(paddedBbox, res)`, PRE-FILTER zones to padded bbox + selected vertical before the inner loop (AC-013), per cell `coverage = count of same-vertical sites where effectiveRadiusMi(site)>0 && haversineMi(cellToLatLng(cell),{lat,lng}) <= effectiveRadiusMi(site)` — overlap-weighted count (AC-004), 0 outside all zones (AC-005), other-vertical never contributes (AC-006), off/null/zero never contribute via effectiveRadiusMi (AC-007), customer_id/self_conflict IGNORED (AC-008), centroid-in-circle boundary correct at 0.4 vs 0.6 for 0.5mi (AC-009); HARD cell-count CAP — above cap return capped/empty + cap flag (reject CONUS-scale low-zoom bbox; metro bbox passes) for the "Zoom in to compute" UI (AC-011); prospect rank — `coverage===0` cells ranked by distance to viewport center, top-N (AC-015). NO supabase.rpc/from, no SQL (AC-030). Unit-test all.

## AS-T3 — saturationLayer + prospectLayer (H3HexagonLayer builders) · depends_on: [AS-T2]
**Files:** src/components/saturationLayer.ts, src/components/saturationLayer.test.ts · **ACs:** AC-014,017 · **Gates:** code-reviewer, performance-reviewer, ui-review
`src/components/saturationLayer.ts` mirroring the siteZonesLayer factory; import `H3HexagonLayer` from the `deck.gl` umbrella (like ScatterplotLayer, AC-028). `saturationLayer(cells)` (AC-017): getHexagon d=>d.h3, extruded:false, filled:true, stroked:false, pickable:false, data filtered to coverage>=1 (open excluded), getFillColor by d.coverage discrete Blues — 1→[198,219,239,150], 2→[107,174,214,170], >=3→[21,88,176,190] (4 clamps to 3+). NO gradient, NO red, NO elevation. `prospectLayer(openCells)`: green outline — getFillColor [19,115,51,35], stroked:true, getLineColor [19,115,51,230], getLineWidth 2, lineWidthUnits 'pixels'. Both updateTriggers keyed [selectedVertical, dataVersion, resolution] (AC-014). saturationLayer.test.ts asserts fill RGBA for coverage 1/2/3/4 + filters coverage 0; prospect line/fill RGBA. (test file = planned addition beyond spec list.)

## AS-T4 — MapShell mount + debounced moveend recompute · depends_on: [AS-T3]
**Files:** src/components/MapShell.tsx · **ACs:** AC-012,018,019,020 · **Gates:** code-reviewer, performance-reviewer, ui-review
Extend overlay.setProps seam (:64-68). New props: cells, openCells, selectedVertical, showHeatmap, showProspecting (+ optional flyToTarget for AS-T6). Mount z-order `[saturationLayer(cells), prospectLayer(openCells), siteZonesLayer(sites, conflictIds), sitePinsLayer(sites)]` — wash under open-outlines under zones under pins (AC-018 saturation first; AC-019 prospect between). CONDITIONAL omit (AC-020): spread saturationLayer only when showHeatmap && selectedVertical; prospectLayer only when showProspecting && selectedVertical — omit entirely (first paint byte-identical to W3). Add debounced (~200ms) moveend → onViewportChange(bounds, zoom, center) — single binding, never per-frame (AC-012). Preserve role="application".

## AS-T5 — SaturationPanel (floating top-right) + index.css · depends_on: []
**Files:** src/components/SaturationPanel.tsx, src/index.css · **ACs:** AC-022..027,029 · **Gates:** code-reviewer, accessibility-auditor, ui-review
New floating top-right `.saturation-panel` (mirrors .site-panel glass; left CRUD panel untouched). CONTROLLED component (state lifted in AS-T6). Vertical `<select>` over VERTICAL_OPTIONS imported from customers.ts (don't re-author), default "Select vertical…" (value "" → null), useId label (AC-022). Toggles (AC-023): native checkboxes useId — "Show saturation heatmap" (default on) + "Highlight open areas" (default off), both native `disabled` while selectedVertical===null. Legend (AC-024): `<ul.sat-legend>` swatch(aria-hidden, #555 hairline)+REAL numeric text: Open (0 zones)/#137333 outline, 1 zone/#c6dbef, 2 zones/#6baed6, 3+ zones/#1558b0 — never color-alone. Summary (AC-025): `.helper-text aria-live=polite` "Saturation for {verticalLabel}: {covered} covered, {open} open near center." States (AC-026): same line carries "Computing saturation…"/"Zoom in to compute saturation"/"Select a vertical…"/"No {vertical} zones in this area — all open." Jump button `<button.btn-secondary>Jump to nearest open area` (disabled when no open cells/no vertical; pan wired AS-T6). Native controls, :focus-visible inherited, no div-onClick (AC-027). index.css (AC-029): .saturation-panel/.sat-legend/.sat-legend__swatch reusing .field/.field-checkbox/.helper-text/.btn-secondary; ONLY new hex = #c6dbef/#6baed6; dynamic legend-swatch bg is the one sanctioned inline style.

## AS-T6 — App wiring + cells derivation + jump-to-open · depends_on: [AS-T2, AS-T4, AS-T5]
**Files:** src/App.tsx · **ACs:** AC-016,021 · **Gates:** code-reviewer, performance-reviewer, accessibility-auditor, ui-review
App owns cells derivation (alongside the conflictIds lifted-state pattern). Lift selectedVertical:string|null, showHeatmap(true), showProspecting(false), viewport (from MapShell onViewportChange). Derive via coverage.ts compute reacting to viewport + selectedVertical + version (data reload) → {cells, openCells, coveredCount, openCount, capped, computing}; recompute on change, never per-frame. Thread cells/openCells + flags + selectedVertical to MapShell; counts/state + selectedVertical/flags + setters to a newly-RENDERED SaturationPanel. AC-021: panel renders (top:3rem;right:1rem), state lifted by conflictIds, git diff shows NO change to CustomerList/CustomerForm/.site-panel. AC-016 (optional): jump button → ease map to nearest coverage===0 cell centroid (MapShell flyToTarget) + announce "Centered on nearest open area." via aria-live; disabled when no open cells/no vertical.
