# Wave 6 — embed-harden (deferred until attach)

**Status:** plan-only (deferred).

**Ships:** ready to mount inside the parent application.

- Implement the real auth provider against the parent app; finalize the API contract.
- AK/HI coverage if needed; true USPS ZIP boundaries if the ZCTA approximation isn't enough.

**Trigger:** when the parent-app integration is actually scoped.

Depends on: Waves 1–5.
